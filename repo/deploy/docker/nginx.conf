server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Vue Router history mode — serve index.html for all unknown routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API calls to backend (used only when frontend container proxies directly)
    location /api/ {
        # Backend terminates TLS (self-signed in dev/test). We skip verification
        # on the internal compose network — the proxy is the only client, both
        # ends of the hop live in the same docker network, and the backend
        # cert is a local ephemeral one.
        proxy_pass https://backend:8080;
        proxy_ssl_verify off;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # Proxy health endpoint to backend
    location /health {
        # Backend terminates TLS (self-signed in dev/test). We skip verification
        # on the internal compose network — the proxy is the only client, both
        # ends of the hop live in the same docker network, and the backend
        # cert is a local ephemeral one.
        proxy_pass https://backend:8080;
        proxy_ssl_verify off;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # Static asset caching
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
